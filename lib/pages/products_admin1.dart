import 'package:flutter/material.dart';

class ProductsAdmin1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // 76.1 make tabbar 
    return Scaffold(
      appBar: AppBar(
        title: Text('Admin Products 1'),
      ),
      body: Center(
        child: Text('welcom to admin page 1'),
      ),
    );
  }
}
